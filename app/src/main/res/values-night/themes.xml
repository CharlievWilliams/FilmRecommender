<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FilmRecommender" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/classic_pink</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/classic_purple</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/classic_pink</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimaryDark">@color/classic_pink</item>
        <item name="android:background">@drawable/splash_background_gradient</item>
        <item name="android:navigationBarColor">@color/classic_pink</item>
    </style>

</resources>